<div class="picture_sidebar filter_price">
  <a href="{{section.settings.banner_url}}">
    {% if section.settings.banner_img != blank %}
    <img src="{{section.settings.banner_img|img_url:'master'}}" class="img-fluid" alt="sidebar">
    {% else %}
    {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
    {% endif %}
  </a>
</div>